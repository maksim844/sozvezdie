<h3><%= @title %></h3>
    <div style="padding:10px 0px 0px 0px" id="galery">
      <% @photos.each do |p| %>
        <div class="galb album">
          <%= link_to image_tag(p.photo.url(:thumb)) %>
        </div>
      <% end %>
    </div>
  <br/>
<div class="clear"></div>
  <%= link_to "К списку альбомов", picturealbum_path(params[:section]) %>
